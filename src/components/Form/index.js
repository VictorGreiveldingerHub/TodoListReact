import React from 'react';
import PropTypes from 'prop-types';


const Form = () => {
  
  return (
    <div>TODO: Implémenter le Formulaire</div>
  );
};

Form.propTypes = {
};

export default Form;
